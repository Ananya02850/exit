//Exit- It is used to terminate the program

#include<stdio.h>
#include<stdlib.h>
int main()
{
  int i,j;
  for(i=1;i<=12;i++)
  {
    if(i==9)
    exit(0);
    printf("%d ", i);
  }
   printf("\nhello");
  
 }
